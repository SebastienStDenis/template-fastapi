# FastAPI Template

## Commands
- `gh repo create my-new-service --template sebastienstdenis/template-fastapi --private --clone` — create a new repository from this template
- `uv sync` — install dependencies
- `uv add <package>` — add a dependency
- `uv run fastapi dev main.py` — run the dev server
- `uv run pytest` — run tests
